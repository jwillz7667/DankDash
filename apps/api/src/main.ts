/**
 * apps/api entrypoint.
 *
 * The NestJS application factory, Fastify adapter wiring, and module
 * graph are introduced in Phase 2 (Auth & Identity). This file exists
 * so the workspace builds and typechecks during Phase 0.
 */
export {};
