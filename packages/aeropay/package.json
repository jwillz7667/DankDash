{
  "name": "@dankdash/aeropay",
  "version": "0.0.0",
  "private": true,
  "description": "Aeropay ACH payment provider adapter — OAuth client-credentials with Redis-cached token, typed REST client wrapping createPayment/getPayment/cancelPayment/refundPayment/createPayout/linkBankAccount, and HMAC webhook signature verification. Wraps undici so the rest of the codebase never imports a raw HTTP client.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dankdash/types": "workspace:*",
    "undici": "^7.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
