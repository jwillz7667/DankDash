{
  "name": "@dankdash/metrc",
  "version": "0.0.0",
  "private": true,
  "description": "Metrc seed-to-sale traceability adapter — Basic-auth client wrapping createReceipt / getActiveReceipts / getReceipt with retry-aware HTTP transport and Zod-validated responses. Per spec §7.2 every delivered order produces a Metrc sales receipt and nightly reconciliation cross-checks /receipts/active against our orders table.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dankdash/types": "workspace:*",
    "undici": "^7.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
